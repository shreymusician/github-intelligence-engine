# Data Directory

Local, gitignored storage for raw and processed repository data.

Subdirectory structure (`raw/`, `processed/`, `features/`, `models/`, `cache/`) is
established in Checkpoint 0.3 alongside the rest of the module scaffolding —
see `IMPLEMENTATION_ROADMAP.md`.
